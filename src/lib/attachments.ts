/** Shared attachment limits / accept list (display + client file picker). */

export const ATTACHMENT_MAX_BYTES_DEFAULT = 10 * 1024 * 1024;
export const ATTACHMENT_MAX_PER_MEETING = 20;

export const ATTACHMENT_ACCEPT =
  ".pdf,.png,.jpg,.jpeg,.gif,.webp,.doc,.docx,.xls,.xlsx,.ppt,.pptx";

export const ATTACHMENT_ACCEPT_FA =
  "PDF، تصویر (JPEG/PNG/GIF/WebP) یا فایل آفیس — سقف ۱۰ مگابایت";
